# 安全政策

SDUMeet 处理学号、照片、性取向等敏感个人信息，安全是项目的生命线。请通过私下渠道上报安全问题，**不要在公开 Issue 中披露漏洞细节**。

## 上报方式

- 邮件：security@sdumeet.example.com（占位，发布前替换为真实邮箱）
- 报告请包含：影响范围、复现步骤、可能的修复建议、你的联系方式

我们承诺：

1. 72 小时内确认收到；
2. 一般问题 7 天内修复，严重问题 48 小时内紧急修复；
3. 修复后经你同意公开致谢。

## 数据处理承诺

- **最小化收集**：只收集完成匹配必需的信息；
- **敏感信息单独同意**：学号、性取向、照片等按《个人信息保护法》要求取得单独授权；
- **可删除**：一键注销后 30 天内物理删除数据；
- **加密**：传输全链路 HTTPS/WSS，数据库静态加密，密钥不进仓库；
- **隔离**：实名数据与业务数据分库分权限，开发环境不使用真实数据。

## 开发者自查清单

- 提交前执行 `git diff --check` 与 `pnpm typecheck`；
- 不提交 `.env`（已在 `.gitignore`）；
- 依赖升级及时跟进安全公告（可启用 Dependabot）；
- 所有涉及用户数据的日志必须脱敏。
